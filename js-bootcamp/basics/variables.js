// 1. You can't define a variable more than once in the same scope
let petName = 'Lotus';

petName = 'Remmi';

console.log(petName);

// 2. There are rules related to variable names
let a = 2;
let result = 3 + 4;

// 3. Variable names cannot be reserved keywords