// === - equality operator
// ==! - not equal operator
// < - less than operator
// > - greater than operator
// <= - less than or equal to operator
// >= - greater than or equal to operator

let temp = 111;
let isFreezing = temp <= 32;

if (temp <= 32) {
    console.log('It is freezing outside!');
}

if (temp >= 110) {
    console.log('It is way too hot outside!');
}



// Challenge area

// Create an age variable set to your age
// Calculate isChild - if they are 7 or under
// Calculate isSenior - if they are 65 or older
// print isChild value
// print isSenior value

let age = 5;

if (age <= 7) {
    console.log('Provide child pricing');
}

if (age >= 65) {
    console.log('Provide senior discount');
}